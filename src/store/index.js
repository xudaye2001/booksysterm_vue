import Vue from 'vue'
import Vuex from 'vuex'

Vue.use(Vuex)

export default new Vuex.Store({
  state: {
    // booklist:[],
  },
  mutations: {
    // addToBookList(state, book) {
    //   this.boolist.push()
    // }
  },
  actions: {
  },
  modules: {
  }
})
